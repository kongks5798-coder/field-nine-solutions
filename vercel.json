{
  "framework": "nextjs",
  "buildCommand": "npm run prisma:generate && npm run build",
  "installCommand": "npm install",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30,
      "runtime": "nodejs20.x"
    }
  }
}
