# ════════════════════════════════════════════════════════════
#  Dalkak (FieldNine) — 환경변수 예시 (.env.local에 복사 후 값 입력)
#  V1 상용화 기준 — 2026-02-24 업데이트
# ════════════════════════════════════════════════════════════

# ── Supabase (필수) ──────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
SUPABASE_DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# ── App URL (필수) ───────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://fieldnine.io
# NEXT_PUBLIC_SITE_URL — NEXT_PUBLIC_APP_URL 대체로 사용 가능
# NEXT_PUBLIC_SITE_URL=https://fieldnine.io

# ── App 버전 (선택, 기본값: 1.0.0) ─────────────────────────
# 헬스체크 및 로거에서 표시
# NEXT_PUBLIC_APP_VERSION=1.0.0

# ── Google OAuth (NextAuth) ──────────────────────────────────
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ── Email ─────────────────────────────────────────────────────
EMAIL_FROM=noreply@fieldnine.io
# CNAME_TARGET=cname.fieldnine.io

# ── TossPayments (한국 결제) ─────────────────────────────────
# test_ck_ = sandbox, live_ck_ = production (반드시 구분!)
# 테스트: https://developers.tosspayments.com → 내 개발 정보
# 라이브: 사업자 심사 완료 후 발급
NEXT_PUBLIC_TOSSPAYMENTS_CLIENT_KEY=test_ck_...   # 또는 live_ck_...
TOSSPAYMENTS_SECRET_KEY=test_sk_...               # 또는 live_sk_...
TOSSPAYMENTS_WEBHOOK_SECRET=whsec_...             # 웹훅 서명 검증용 (TossPayments 콘솔 → 웹훅 설정에서 발급)

# ── Stripe (해외 카드 결제) ──────────────────────────────────
# https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...         # 또는 sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...       # Webhook 등록 후 발급
# Stripe 플랜별 가격 ID (Stripe Dashboard → Products에서 생성)
# STRIPE_PRICE_PRO_MONTHLY=price_...
# STRIPE_PRICE_TEAM_MONTHLY=price_...

# ── Polar 결제 (선택 — Stripe 대안) ─────────────────────────
# Polar 체크아웃 URL (Polar 대시보드에서 발급)
# POLAR_CHECKOUT_URL_PRO=https://polar.sh/checkout/...
# POLAR_CHECKOUT_URL_TEAM=https://polar.sh/checkout/...

# ── AI 모델 (선택 — 없으면 해당 모델 비활성화) ────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_GENERATIVE_AI_API_KEY=AIza...
XAI_API_KEY=xai-...                   # Grok AI

# ── 이메일 (Resend) ──────────────────────────────────────────
# https://resend.com → API Keys
RESEND_API_KEY=re_...

# ── 모니터링 (선택 — 없어도 서비스 동작) ─────────────────────
# Sentry: https://sentry.io → 새 프로젝트 → Next.js → DSN 복사
SENTRY_DSN=https://...@sentry.io/...
SENTRY_AUTH_TOKEN=sntrys_...           # Source map 업로드용 (선택)

# PostHog: https://posthog.com → Project Settings → API Key
NEXT_PUBLIC_POSTHOG_KEY=phc_...
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ── Rate Limiting (Upstash Redis, 선택) ───────────────────────
# https://upstash.com → Create Database → REST API
# 없으면 in-memory fallback 사용 (단일 인스턴스만 유효)
UPSTASH_REDIS_REST_URL=https://...upstash.io
UPSTASH_REDIS_REST_TOKEN=AX...

# ── Cron 보호 (선택) ─────────────────────────────────────────
# 랜덤 32자 문자열: openssl rand -hex 32
CRON_SECRET=your-random-secret-here

# ── Admin / 인증 (선택) ─────────────────────────────────────
ADMIN_SECRET=your-admin-secret
# ADMIN_PASSWORD — 관리자 로그인 비밀번호
# ADMIN_PASSWORD=your-admin-password
# ADMIN_2FA_CODE — 관리자 2단계 인증 코드 (선택)
# ADMIN_2FA_CODE=your-2fa-code
# JWT_SECRET — JWT 토큰 서명용 (선택, SESSION_SECRET 대체 가능)
# JWT_SECRET=your-jwt-secret
# SESSION_SECRET — 세션 서명용 (선택, JWT_SECRET 대체 가능)
# SESSION_SECRET=your-session-secret

# ── CSRF 보호 (선택, 기본값 사용 가능) ───────────────────────
# CSRF_SECRET=your-csrf-secret

# ── 사이트 게이트 비밀번호 (선택 — 비공개 베타 시 사용) ──────
# /gate 페이지에서 사이트 접근 비밀번호 요구
# SITE_GATE_PASSWORD=your-gate-password

# ── Linear 이슈 트래커 (선택 — 에러 리포트 연동) ────────────
# https://linear.app → Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_...
LINEAR_TEAM_ID=your-linear-team-id

# ── Slack 알림 (선택 — 에러/알림 연동) ──────────────────────
# SLACK_ALERT_WEBHOOK_URL — 에러 알림용 웹훅
# SLACK_ALERT_WEBHOOK_URL=https://hooks.slack.com/services/...
# SLACK_WEBHOOK_URL — 일반 웹훅 (통합 상태 체크에서 사용)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
# SLACK_BOT_TOKEN — Bot 토큰 (선택)
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_CHANNEL_ID — 알림 채널 ID (선택)
# SLACK_CHANNEL_ID=C...

# ── Zapier (선택 — 자동화 연동) ──────────────────────────────
# ZAPIER_WEBHOOK_URL=https://hooks.zapier.com/...

# ── 오브젝트 스토리지 (선택) ─────────────────────────────────
# Supabase Storage 버킷 ID
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id

# ── DB 프로바이더 (선택, 기본값: memory) ─────────────────────
# supabase | memory (통합 상태 체크에서 사용)
DB_PROVIDER=supabase
